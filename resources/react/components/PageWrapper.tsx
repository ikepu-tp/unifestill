import { PageWrapper as BasePageWrapper, PageWrapperProps } from '@ikepu-tp/react-bootstrap-extender';

export default function PageWrapper(props: PageWrapperProps): JSX.Element {
	return <BasePageWrapper {...props} />;
}
